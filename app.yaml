service: configs
owner: he110
description: Сервис централизованной конфигурации
type: business
organization: stream-gpt
port: 8080
env:
    HEALTH_CHECK_ENABLED: true
    LOG_LEVEL: info
    METRICS_ENABLED: true
    SERVICE_VERSION: 1.0.0
env.staging:
    LOG_LEVEL: debug
env.production:
    LOG_LEVEL: warn
secrets:
    - SOME_SECRET_VAR

secrets:
  - SOME_SECRET_VAR # Пример того, как перечислять секреты для сервиса
                    # Секреты должны быть записаны в Secrets в GitHub репозитории одним файлом/значением по ключу SECRETS

# gateway: /my-awesome-service # Пример того, как можно сделать сервис доступным из вне

dependencies:
  # Примеры зависимостей - раскомментируйте и настройте под ваш сервис
  
  # База данных
  # - type: database
  #   name: postgres
  #   version: 15
  #   port: 5432
  #   database: configs_db

  # Кэш
  # - type: cache
  #   name: redis
  #   version: 7.0
  #   port: 6379

  # Очередь сообщений
  # - type: queue
  #   name: rabbitmq
  #   version: 3.12
  #   port: 5672

  # Другой сервис
  # - type: service
  #   name: auth-service
  #   port: 8080
  #   path: /api/v1
